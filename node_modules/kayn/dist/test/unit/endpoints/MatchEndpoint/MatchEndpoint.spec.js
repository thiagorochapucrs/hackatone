'use strict';

var _chai = require('chai');

var _TestUtils = require('../../../TestUtils');

var _TestUtils2 = _interopRequireDefault(_TestUtils);

var _MatchEndpoint = require('../../../../lib/Endpoints/MatchEndpoint/MatchEndpoint');

var _MatchEndpoint2 = _interopRequireDefault(_MatchEndpoint);

var _mocks = require('../../../mocks');

var _mocks2 = _interopRequireDefault(_mocks);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var kaynInstance = _TestUtils2.default.kaynInstance,
    defaultConfig = _TestUtils2.default.defaultConfig;
var kayn = kaynInstance.kayn,
    REGIONS = kaynInstance.REGIONS,
    METHOD_TYPES = kaynInstance.METHOD_TYPES;


describe('MatchEndpoint', function () {
  this.timeout(0);

  beforeEach(function () {
    this.Match = new _MatchEndpoint2.default(defaultConfig);
  });

  describe('.get', function () {
    it('should have the correct payload #1', function () {
      var id = _mocks2.default.match.id;

      var _Match$get = this.Match.get(id),
          payload = _Match$get.payload;

      (0, _chai.expect)(payload).to.deep.equal({
        method: 'GET',
        serviceName: 'match',
        endpoint: 'matches/' + id,
        query: [],
        region: ''
      });
    });
  });

  describe('.timeline', function () {
    it('should have the correct payload #1', function () {
      var id = _mocks2.default.match.id;

      var _Match$timeline = this.Match.timeline(id),
          payload = _Match$timeline.payload;

      (0, _chai.expect)(payload).to.deep.equal({
        method: 'GET',
        serviceName: 'match',
        endpoint: 'timelines/by-match/' + id,
        query: [],
        region: ''
      });
    });
  });
});